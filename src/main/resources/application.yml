server:
  port: 8761

spring:
  application:
    name: discovery-server
  cloud:
    config:
      enabled: false

groq:
  api:
    key: ${GROQ_API_KEY}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost